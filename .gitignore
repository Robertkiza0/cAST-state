__pycache__/
*.pyc

# Données locales volumineuses (dépôts source, tâches RepoCoder/CCEval, cache de chunks) —
# reproductibles via les notebooks Colab (téléchargent leurs sources à la volée),
# pas versionnées ici.
data/
datasets rapo/
crosscodeeval_data/
cceval_repos/

# Sorties de run_benchmark.py (résultats par tâche + résumés agrégés) —
# générées à l'exécution, pas du code.
results/

# Clones de référence en lecture seule (code tiers public, pas notre travail) —
# gardés sur disque pour lecture/dépendance, pas commités dans ce dépôt.
# astchunk: https://github.com/yilinjz/astchunk (aussi sur PyPI: pip install astchunk)
# cceval: https://github.com/amazon-science/cceval (harnais officiel CrossCodeEval)
astchunk_reference/
cceval/

.env
